# Server Configuration: Bind to cloud host $PORT (defaults to 8080 locally)
server.port=${PORT:8080}

# Spring Boot Web Configuration
spring.application.name=JavaBank Web Application
spring.web.resources.static-locations=classpath:/static/
